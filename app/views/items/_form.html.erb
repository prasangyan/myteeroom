<%= form_for @item, :html => { :multipart => true }  do |f| %>
  <% if @item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@item.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
      <% @item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
	<div class="field">
    <%= f.label :price %><br />
    <%= f.text_field :price %>
  </div>
 	<div>
		<%= f.label :thumbnail %>
		<%= f.file_field :thumbnail %>
 	</div>
 	<div>
		<%= f.label :large_preview%>
		<%= f.file_field :large_preview %>
 	</div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
